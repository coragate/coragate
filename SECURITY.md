# Security

Please report vulnerabilities privately. Do not open a public issue or pull request with an exploit.

Until a dedicated security contact is published, email the maintainer listed in `git log` / GitHub org owners.

This project is an inline LLM proxy. Reports that involve leaking upstream credentials, bypassing `enforce` in a way that skips audit, or injecting into the SSE path are especially in scope.
